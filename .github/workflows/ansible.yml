
name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  lint:
    runs-on: self-hosted
    steps:
      uses: actions/checkout@v2
      uses: ./.github/actions/ansible
      run: |
        cat /etc/hostname
        ansible-lint run.yml
